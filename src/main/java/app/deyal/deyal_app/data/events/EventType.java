package app.deyal.deyal_app.data.events;

public enum EventType {
    CREATE,
    UPDATE,
    PUBLISH,
    REQUEST,
    ASSIGN,
    SUBMIT,
    APPROVE,
    REJECT,
    REVIEW;

    EventType() {
    }
}
